{
  "name": "anime-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "build": "esbuild api/anime/info.js --bundle --platform=neutral --target=es2020 --outfile=dist/index.js",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}